import { Input, InputGroup, InputLeftElement } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <Input borderRadius={20} placeholder="Search Plants..." variant="filled" />
  );
};

export default SearchInput;
